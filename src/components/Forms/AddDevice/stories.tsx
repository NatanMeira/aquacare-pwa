import { Story, Meta } from '@storybook/react'
import AddDevice from '.'

export default {
  title: 'Forms/AddDevice',
  component: AddDevice
} as Meta

export const Default: Story = () => <AddDevice />
